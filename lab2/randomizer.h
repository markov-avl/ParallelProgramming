#ifndef PARALLELPROGRAMMING_RANDOMIZER_H
#define PARALLELPROGRAMMING_RANDOMIZER_H

#include <iostream>

double randomizedAverageOmp(uint64_t, unsigned *, size_t, unsigned, unsigned);

double randomizedAverageCpp(uint64_t, unsigned *, size_t, unsigned, unsigned);

#endif
